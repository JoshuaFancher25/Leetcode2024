# practice creating a function
def this_func(a: int, b: int) -> int:
    return a + b

print(this_func(10,15))

# must use the semicolon after all the optional parameters
# the optional arguments come after the semicolon in the parameters 